#!/bin/bash

3 terminal windows

$ docker stats
$ docker run --rm -it --privileged -p 8080:80 denhamparry/exploit001:0.0.1
$ curl localhost:8080

# Visit in browser

$ ./exploit.sh localhost:8080

# What is a CVE?

# The common vulnerabilities and exposures (CVE) program.
# Cataloging software and firmware vulnerabilities.
# Made public once fixes have been put in place.
# Look at them to test out youself.
# All this is in the git repo with the slides.

$ whoami
$ pwd
$ ls
$ ps