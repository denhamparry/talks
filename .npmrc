# NPM Configuration
# Enforces consistent package installations across environments

# Enforce engine requirements from package.json
engine-strict=true

# Save exact versions (no semver ranges like ^1.0.0)
save-exact=true
